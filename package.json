{
  "name": "bdzteam-key-system",
  "private": true,
  "scripts": {
    "check": "node --check api/_lib.js && node --check api/check-key.js && node --check api/get-key.js && node --check api/shorten.js && node --check api/admin.js && node --check api/check-version.js"
  },
  "engines": { "node": ">=20" }
}
