(()=>{'use strict';
const ENDPOINT='https://nklukqriopezsoalnghm.supabase.co/functions/v1/check-key';
const EX={
java:`String endpoint="${ENDPOINT}";\nString body="{\\"key\\":\\"YOUR_KEY\\",\\"product_slug\\":\\"YOUR_SLUG\\"}";\nHttpRequest request=HttpRequest.newBuilder(URI.create(endpoint)).header("Content-Type","application/json").POST(HttpRequest.BodyPublishers.ofString(body)).build();\nHttpResponse<String> response=HttpClient.newHttpClient().send(request,HttpResponse.BodyHandlers.ofString());\n// Open the App menu only when response status is VALID.`,
cpp:`std::string endpoint="${ENDPOINT}";\nstd::string body=R"({"key":"YOUR_KEY","product_slug":"YOUR_SLUG"})";\n// POST JSON with libcurl. Only continue when the JSON field status == VALID.`,
python:`import requests\nresponse=requests.post("${ENDPOINT}",json={"key":"YOUR_KEY","product_slug":"YOUR_SLUG"},timeout=10)\nprint(response.json())`,
html:`async function checkKey(key,productSlug,deviceId=null){\n const body={key,product_slug:productSlug};\n if(deviceId) body.device_id=deviceId;\n const r=await fetch("${ENDPOINT}",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});\n return await r.json();\n}`,
php:`$payload=json_encode(["key"=>"YOUR_KEY","product_slug"=>"YOUR_SLUG"]);\n$ch=curl_init("${ENDPOINT}");\ncurl_setopt_array($ch,[CURLOPT_POST=>true,CURLOPT_HTTPHEADER=>['Content-Type: application/json'],CURLOPT_POSTFIELDS=>$payload,CURLOPT_RETURNTRANSFER=>true,CURLOPT_TIMEOUT=>10]);\necho curl_exec($ch);curl_close($ch);`
};
const RESPONSE={ok:true,status:'VALID',code:'VALID',key:'BDZ-XXXX',product:'Example',product_slug:'example',expires_at:'2026-09-01T12:00:00Z',claimed_ip:'1.2.3.4',activated_at:'2026-09-01T11:00:00Z',device_bound:false,devices_used:0,max_devices:1};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const copy=async v=>{try{await navigator.clipboard.writeText(v);return true}catch{return false}};
function mount(){const title=document.querySelector('#title'),panel=document.querySelector('#panel');if(!title||!panel||title.textContent.trim()!=='API Key')return;panel.innerHTML=`<section class="panel"><div class="panel-head"><div><h2>API Key · Supabase</h2><p>APK/Menu kiểm tra Key trực tiếp qua Supabase Edge Function.</p></div></div><div class="endpoint"><strong>POST</strong><code>${ENDPOINT}</code><button class="btn sm" id="apiCopyEndpoint">Copy</button></div><div class="metrics" style="margin-top:12px"><div class="metric"><b>key</b><span>Key người dùng nhập</span></div><div class="metric"><b>product_slug</b><span>Slug của game/app</span></div><div class="metric"><b>device_id*</b><span>Chỉ gửi khi Key do Admin tạo và cần giới hạn thiết bị.</span></div><div class="metric"><b>Không có IP</b><span>API verification không dùng IP để quyết định hợp lệ.</span></div><div class="metric"><b>max_devices</b><span>Giới hạn thiết bị của Key Admin.</span></div></div><p style="font-size:10px;color:#7b8795;margin-top:10px">* Key Get Key chỉ dùng 1 lần thành công rồi chuyển sang USED. Key Admin quản lý theo số thiết bị.</p></section><section class="panel"><div class="panel-head"><div><h2>Request · Get Key</h2><p>Chỉ cần Key + Product/Slug.</p></div></div><div class="code"><pre>${esc(JSON.stringify({key:'YOUR_KEY',product_slug:'YOUR_SLUG'},null,2))}</pre></div></section><section class="panel"><div class="panel-head"><div><h2>Request · Admin Key</h2><p>Gửi thêm device_id để server đếm thiết bị đã đăng ký.</p></div></div><div class="code"><pre>${esc(JSON.stringify({key:'ADMIN_KEY',product_slug:'YOUR_SLUG',device_id:'YOUR_STABLE_DEVICE_ID'},null,2))}</pre></div></section><section class="panel"><div class="panel-head"><div><h2>Response mẫu</h2><p>Chỉ mở menu khi <code>status=VALID</code>.</p></div></div><div class="code"><pre>${esc(JSON.stringify(RESPONSE,null,2))}</pre></div><p style="font-size:10px;color:#7b8795">Trạng thái: VALID, USED, INVALID, EXPIRED, REVOKED, DEVICE_REQUIRED, DEVICE_LIMIT, SERVER_ERROR.</p></section><section class="panel"><div class="panel-head"><div><h2>Examples / Ví dụ tích hợp</h2><p>Java · C++ · Python · HTML · PHP</p></div></div><div class="tabs">${Object.keys(EX).map(x=>`<button class="${(window.__bdzApiExample||'java')===x?'active':''}" data-api-example="${x}">${x.toUpperCase()}</button>`).join('')}</div><div class="code"><button class="btn sm copy" id="apiCopyExample">Copy code</button><pre id="apiExampleCode">${esc(EX[window.__bdzApiExample||'java'])}</pre></div></section>`;document.querySelector('#apiCopyEndpoint')?.addEventListener('click',async e=>{if(await copy(ENDPOINT))e.currentTarget.textContent='Copied'});document.querySelectorAll('[data-api-example]').forEach(b=>b.addEventListener('click',()=>{window.__bdzApiExample=b.dataset.apiExample;mount()}));document.querySelector('#apiCopyExample')?.addEventListener('click',async()=>{if(await copy(EX[window.__bdzApiExample||'java']))document.querySelector('#apiCopyExample').textContent='Copied'});}
const obs=new MutationObserver(()=>setTimeout(mount,0));obs.observe(document.body,{subtree:true,childList:true});document.addEventListener('DOMContentLoaded',mount);mount();
})();
