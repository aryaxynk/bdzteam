const $=s=>document.querySelector(s),params=new URLSearchParams(location.search),token=params.get('token'),step=params.get('step');
const err=m=>{const x=$('#error');if(x){x.textContent=m||'';x.classList.toggle('hidden',!m)}};
const setStep=n=>[1,2,3,4].forEach(i=>$('#s'+i)?.classList.toggle('active',i===n));
const show=id=>['choose','verify','claimed'].forEach(x=>$('#'+x)?.classList.toggle('hidden',x!==id));
async function loadProducts(){const r=await fetch('/api/products');const d=await r.json();if(!r.ok)throw Error(d.error||'Không tải được sản phẩm');const box=$('#products');box.innerHTML='';(d.products||[]).forEach((p,i)=>{const b=document.createElement('button');b.className='btn product'+(i===0?' selected':'');b.dataset.id=p.id;b.innerHTML='<h3>'+String(p.name).replaceAll('<','&lt;')+'</h3><span class="small muted">'+String(p.slug).replaceAll('<','&lt;')+'</span>';b.onclick=()=>{document.querySelectorAll('.product').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')};box.appendChild(b)})}
async function start(){err('');const b=$('#start'),selected=document.querySelector('.product.selected');if(!selected)return err('Vui lòng chọn sản phẩm.');b.disabled=true;b.textContent='Đang tạo link...';try{const r=await fetch('/api/start-get-key',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({product_id:Number(selected.dataset.id)})});const d=await r.json();if(!r.ok)throw Error(d.error||'Không thể bắt đầu');location.href=d.redirect}catch(e){err(e.message);b.disabled=false;b.textContent='Tiếp tục đến trang vượt link'}}
async function claim(){err('');const b=$('#claim');b.disabled=true;b.textContent='Đang cấp Key...';try{const r=await fetch('/api/claim-key',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token})});const d=await r.json();if(!r.ok)throw Error(d.error||'Không thể nhận Key');$('#key').textContent=d.key_code;$('#expires').textContent='Hết hạn: '+new Date(d.expires_at).toLocaleString('vi-VN');show('claimed');setStep(4)}catch(e){err(e.message);b.disabled=false;b.textContent='Nhận Key'}}
if($('#products'))loadProducts().catch(e=>err(e.message));if($('#start'))$('#start').onclick=start;if(step==='3'&&token){show('verify');setStep(3);$('#claim').onclick=claim}if($('#copy'))$('#copy').onclick=async()=>{await navigator.clipboard.writeText($('#key').textContent);$('#success').textContent='Đã sao chép Key.'};
