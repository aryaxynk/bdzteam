document.addEventListener('DOMContentLoaded',()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const enhance=()=>{document.querySelectorAll('#tabContent .admin-panel').forEach((el,i)=>{el.classList.add('admin-enter');if(!reduce)el.style.animationDelay=Math.min(i*45,320)+'ms'});document.querySelectorAll('#tabContent table tbody tr').forEach((el,i)=>{if(!reduce)el.style.animationDelay=Math.min(i*18,260)+'ms';el.classList.add('admin-row-enter')});document.querySelectorAll('#tabContent button,#tabContent .nb-btn,#tabContent .small-btn').forEach(b=>b.classList.add('admin-interactive'));};const mo=new MutationObserver(()=>enhance());mo.observe(document.getElementById('tabContent')||document.body,{childList:true,subtree:true});enhance();window.addEventListener('beforeunload',()=>mo.disconnect());});
