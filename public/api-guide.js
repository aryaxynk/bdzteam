(()=>{let rendered=false;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const copy=async text=>{try{await navigator.clipboard.writeText(text);return true}catch{return false}};const setTab=()=>{try{if(typeof state!=='undefined')state.tab='integration';document.querySelectorAll('[data-tab]').forEach(x=>x.classList.toggle('active',x.dataset.tab==='integration'));const t=document.getElementById('tabTitle'),s=document.getElementById('tabSub');if(t)t.textContent='Tích Hợp API';if(s)s.textContent='App / Game → Supabase trực tiếp';}catch{}};const wire=()=>{document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{const ok=await copy(b.dataset.copy);const old=b.textContent;b.textContent=ok?'✓ Đã sao chép':'Không thể sao chép';setTimeout(()=>b.textContent=old,1000)}));document.querySelectorAll('[data-code-tab]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-code-tab]').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.api-code-block').forEach(x=>x.classList.remove('active'));document.getElementById(b.dataset.codeTab)?.classList.add('active')}));document.querySelectorAll('[data-step-copy]').forEach(b=>b.addEventListener('click',async()=>{const ok=await copy(b.dataset.stepCopy);const old=b.textContent;b.textContent=ok?'✓ Đã sao chép':'Lỗi';setTimeout(()=>b.textContent=old,1000)}));};const guide=()=>{const c=document.getElementById('tabContent');if(!c)return;setTab();if(rendered&&c.querySelector('.api-guide'))return;const supabase='https://nklukqriopezsoalnghm.supabase.co';const rpc=supabase+'/rest/v1/rpc/verify_key_app';const pub='sb_publishable_RUE5iV8GqoVCFxjVt5ZBpg_FlTC1v-0';const exampleBody='{"p_key_code":"BDZ-XXXXXXXXXXXX","p_client_ip":"1.2.3.4","p_product_slug":"free-fire"}';c.innerHTML=`<div class="space-y-4 api-guide"><section class="admin-panel p-4 md:p-5 api-panel"><div class="flex flex-wrap items-center justify-between gap-2"><div><span class="status-pill success">GAME API · DIRECT SUPABASE</span><h2 class="text-xl md:text-2xl font-black mt-2">Luồng duy nhất cần dùng trong App</h2></div><span class="status-pill info">App ⇄ Supabase</span></div><div class="api-note mt-3"><b>Flow chuẩn:</b> Người dùng lấy Key trên website → nhập Key vào menu App → App gọi trực tiếp Supabase RPC → Supabase kiểm tra → trả JSON → App đọc <code>status</code> → ACTIVE mới cho bước tiếp theo.</div><div class="mt-4 grid md:grid-cols-5 gap-2 text-xs"><div class="step-box active p-3"><b>① GET KEY</b><br>Website cấp Key.</div><div class="step-box p-3"><b>② NHẬP KEY</b><br>App nhận Key.</div><div class="step-box p-3"><b>③ CHECK</b><br>POST Supabase RPC.</div><div class="step-box p-3"><b>④ KẾT QUẢ</b><br>Supabase trả JSON.</div><div class="step-box p-3"><b>⑤ TIẾP TỤC</b><br>ACTIVE → mở menu.</div></div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">1. Thông tin phải copy đúng 100%</h2><div class="mt-3 space-y-3 text-xs"><div><b>Supabase Project URL</b><div class="api-copy"><code>${esc(supabase)}</code><button class="small-btn" data-copy="${esc(supabase)}">Sao chép</button></div></div><div><b>RPC URL</b><div class="api-copy"><code>${esc(rpc)}</code><button class="small-btn" data-copy="${esc(rpc)}">Sao chép</button></div></div><div><b>Publishable key</b><div class="api-copy"><code>${esc(pub)}</code><button class="small-btn" data-copy="${esc(pub)}">Sao chép</button></div></div><div><b>RPC function</b><div class="api-copy"><code>verify_key_app</code><button class="small-btn" data-copy="verify_key_app">Sao chép</button></div></div></div><div class="api-note mt-3"><b>Không đổi tên tham số.</b> Tên chính xác là <code>p_key_code</code>, <code>p_client_ip</code>, <code>p_product_slug</code>. Không gọi Vercel API cho flow của App.</div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">2. Request POST chuẩn — copy vào App</h2><pre class="text-xs mt-3">POST ${rpc}\nContent-Type: application/json\napikey: ${pub}\nAuthorization: Bearer ${pub}\nAccept: application/json\n\n${exampleBody}</pre><div class="api-copy"><button class="small-btn" data-copy="${esc(rpc)}">Copy URL</button><button class="small-btn" data-copy="${esc(exampleBody)}">Copy JSON</button></div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">3. Response thành công</h2><pre class="text-xs mt-3">[\n  {\n    "status": "ACTIVE",\n    "product": "Free Fire",\n    "product_id": 4,\n    "duration_hours": 10,\n    "expires_at": "2026-08-19T10:00:00Z",\n    "claimed_ip": "1.2.3.4",\n    "key_scope": "GET",\n    "activated_at": null,\n    "activated_ip": null,\n    "message": "Key hợp lệ."\n  }\n]</pre><div class="api-note mt-3">REST RPC của PostgreSQL function trả về <b>mảng JSON</b>. App lấy phần tử đầu tiên: <code>response[0]</code>.</div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">4. App quyết định theo status</h2><div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-2 mt-3 text-xs">${[['ACTIVE','Cho phép vào menu/chức năng tiếp theo.'],['INVALID','Key không tồn tại → báo lỗi, dừng.'],['EXPIRED','Key hết hạn → dừng.'],['REVOKED','Admin đã khóa Key → dừng.'],['IP_MISMATCH','IP không khớp Key GET → dừng.'],['WRONG_PRODUCT','Sai game/sản phẩm → dừng.'],['SERVER_ERROR','Supabase tạm lỗi → báo thử lại; không mở menu.'],['HTTP_401/403','Publishable key hoặc quyền RPC sai → kiểm tra cấu hình.'],['HTTP_429','Quá nhiều request → chờ rồi thử lại.']].map(([a,b])=>`<div class="step-box p-3"><b>${esc(a)}</b><div class="mt-1 text-[#666]">${esc(b)}</div></div>`).join('')}</div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">5. Logic trong App — giả mã</h2><pre class="text-xs mt-3">userInput = readKey()\nif userInput.isBlank(): stop("Vui lòng nhập Key")\n\nresult = supabaseVerify(userInput)\nif networkError: stop("Không thể kết nối máy chủ")\nif httpError: stop("Supabase HTTP " + code)\n\nitem = result[0]\nwhen(item.status) {\n  "ACTIVE" -> openNextMenu()\n  "INVALID" -> stop("Key không hợp lệ")\n  "EXPIRED" -> stop("Key đã hết hạn")\n  "REVOKED" -> stop("Key đã bị khóa")\n  "IP_MISMATCH" -> stop("Key không thuộc thiết bị/IP này")\n  "WRONG_PRODUCT" -> stop("Key không dành cho sản phẩm này")\n  else -> stop(item.message ?: "Xác minh thất bại")\n}</pre><div class="api-note mt-3"><b>Quy tắc:</b> không có <code>ACTIVE</code> thì không mở menu tiếp theo.</div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">6. Kotlin — Android Studio / AIDE Pro</h2><div class="code-tabs mt-3"><button class="active" data-code-tab="kt">Kotlin</button><button data-code-tab="java">Java</button></div><div id="kt" class="api-code-block active mt-3"><pre class="text-xs">val supabaseUrl = "${supabase}"
val publishableKey = "${pub}"
val key = "BDZ-XXXXXXXXXXXX"
val clientIp = "1.2.3.4"
val product = "free-fire"
val json = """{"p_key_code":"$key","p_client_ip":"$clientIp","p_product_slug":"$product"}"""
val body = json.toRequestBody("application/json".toMediaType())
val request = Request.Builder()
    .url("$supabaseUrl/rest/v1/rpc/verify_key_app")
    .post(body)
    .header("apikey", publishableKey)
    .header("Authorization", "Bearer $publishableKey")
    .header("Content-Type", "application/json")
    .header("Accept", "application/json")
    .build()
client.newCall(request).enqueue(object : Callback {
    override fun onFailure(call: Call, e: IOException) {
        // Không mở menu khi mất mạng
    }
    override fun onResponse(call: Call, response: Response) {
        val text = response.body?.string().orEmpty()
        // parse JSONArray -> getJSONObject(0)
        // if status == ACTIVE -> continue
    }
})</pre></div><div id="java" class="api-code-block mt-3"><pre class="text-xs">String supabaseUrl = "${supabase}";
String publishableKey = "${pub}";
String bodyJson = "{\"p_key_code\":\"BDZ-XXXXXXXXXXXX\",\"p_client_ip\":\"1.2.3.4\",\"p_product_slug\":\"free-fire\"}";
RequestBody body = RequestBody.create(bodyJson, MediaType.parse("application/json"));
Request request = new Request.Builder()
    .url(supabaseUrl + "/rest/v1/rpc/verify_key_app")
    .post(body)
    .addHeader("apikey", publishableKey)
    .addHeader("Authorization", "Bearer " + publishableKey)
    .addHeader("Content-Type", "application/json")
    .addHeader("Accept", "application/json")
    .build();
client.newCall(request).enqueue(new Callback() {
  @Override public void onFailure(Call call, IOException e) { }
  @Override public void onResponse(Call call, Response response) throws IOException {
    String text = response.body() != null ? response.body().string() : "";
    // Parse JSONArray -> object 0 -> status/message
  }
});</pre></div><p class="text-xs text-[#666] mt-3">OkHttp + JSON parser (org.json/Moshi/Gson). Không gọi mạng trên UI thread. Timeout 10–15 giây; retry tối đa 2 lần cho lỗi mạng/5xx.</p></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">7. Checklist trước khi build APK</h2><div class="grid md:grid-cols-2 gap-2 text-xs"><div class="step-box p-3">☐ Supabase URL đúng</div><div class="step-box p-3">☐ Publishable key đúng</div><div class="step-box p-3">☐ RPC = <code>verify_key_app</code></div><div class="step-box p-3">☐ POST đúng JSON</div><div class="step-box p-3">☐ product_slug đúng slug sản phẩm</div><div class="step-box p-3">☐ Parse mảng JSON lấy phần tử 0</div><div class="step-box p-3">☐ Chỉ ACTIVE mới mở menu</div><div class="step-box p-3">☐ Không nhúng Service Role/Secret</div></div></section><section class="admin-panel p-4 md:p-5 api-panel"><h2 class="text-lg font-black">8. Lưu ý IP</h2><div class="api-note">Do App gọi Supabase trực tiếp, <code>p_client_ip</code> là giá trị App gửi lên. Supabase không thể coi giá trị này là bằng chứng mạnh chống giả mạo. Trường này được giữ để duy trì logic IP binding của hệ thống. Nếu cần chống giả mạo IP mạnh hơn, phải chuyển verify qua một server-side endpoint.</div></section></div>`;wire();rendered=true};const open=e=>{if(e){e.preventDefault();e.stopImmediatePropagation()}rendered=false;setTab();requestAnimationFrame(guide)};document.addEventListener('DOMContentLoaded',()=>document.querySelectorAll('[data-tab="integration"]').forEach(b=>b.addEventListener('click',open,true)));window.BDZApiGuide={render:guide,reset:()=>{rendered=false;guide()}};})();