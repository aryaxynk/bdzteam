import { requireAdmin } from "../../src/admin_api.js";
import { json, sb } from "../../src/db.js";
function send(res,r){res.statusCode=r.status;r.headers.forEach((v,k)=>res.setHeader(k,v));return r.arrayBuffer().then(b=>res.end(Buffer.from(b)))}
function reqObj(req){return new Request(`https://${req.headers.host||"localhost"}${req.url||"/api/admin/notifications"}`,{method:req.method,headers:new Headers(Object.entries(req.headers||{}).reduce((o,[k,v])=>{if(v!=null)o[k]=Array.isArray(v)?v.join(","):String(v);return o},{})),body:['GET','HEAD'].includes(req.method)?undefined:(typeof req.body==='string'?req.body:JSON.stringify(req.body||{}))})}
export default async function handler(req,res){try{const request=reqObj(req),session=await requireAdmin(request,process.env);if(!session)return send(res,json({ok:false,error:'Chưa đăng nhập quản trị.'},401));if(req.method==='GET'){const rows=await sb(process.env,'admin_notifications?select=id,title,message,level,created_by,created_role,is_published,created_at&is_published=eq.true&order=created_at.desc&limit=50');return send(res,json({ok:true,notifications:rows||[]}))}if(req.method==='POST'){const b=await request.json().catch(()=>({})),title=String(b.title||'').trim(),message=String(b.message||'').trim(),level=['info','success','warning','danger'].includes(String(b.level))?String(b.level):'info';if(!title||!message)return send(res,json({ok:false,error:'Tiêu đề và nội dung không được để trống.'},400));if(title.length>120||message.length>2000)return send(res,json({ok:false,error:'Nội dung thông báo quá dài.'},400));await sb(process.env,'admin_notifications',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({title,message,level,created_by:String(session.u),created_role:String(session.r),is_published:true})});return send(res,json({ok:true}))}if(req.method==='DELETE'){const u=new URL(request.url),id=Number(u.searchParams.get('id'));if(!Number.isInteger(id)||id<=0)return send(res,json({ok:false,error:'ID thông báo không hợp lệ.'},400));await sb(process.env,'admin_notifications?id=eq.'+id,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({is_published:false})});return send(res,json({ok:true}))}return send(res,json({ok:false,error:'Method Not Allowed.'},405,{allow:'GET,POST,DELETE'}))}catch(e){return send(res,json({ok:false,error:String(e?.message||e||'Lỗi máy chủ.')},500))}}
