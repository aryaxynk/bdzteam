import {json,ipOf,sha256,rpc,setting,getSlots,banned,autoBanned,securityLog,ensureProducts,sb,consumeRateLimit,registerViolation,isBadUserAgent} from "./db.js";
import {shortenWithProvider} from "./shorteners/index.js";
export async function siteData(env){const [name,slogan,contact,maintenance,message]=await Promise.all([setting(env,"site_name","BDZTEAM"),setting(env,"site_slogan","Nền tảng quản lý và xác thực Key tự động"),setting(env,"contact_info",""),setting(env,"maintenance_enabled","false"),setting(env,"maintenance_message","Website đang bảo trì. Vui lòng quay lại sau.")]);return{site_name:name,site_slogan:slogan,contact_info:contact,maintenance_enabled:maintenance==='true',maintenance_message:message,logo_url:"https://files.catbox.moe/xpaekr.jpg"};}
export async function publicStats(env){const now=encodeURIComponent(new Date().toISOString());const[a,b,c,d]=await Promise.all([sb(env,"keys?select=id&limit=10000").catch(()=>[]),sb(env,"keys?select=id&status=eq.ACTIVE&or=(expires_at.gt."+now+",expires_at.is.null)&limit=10000").catch(()=>[]),sb(env,"logs?select=id&limit=10000").catch(()=>[]),sb(env,"products?select=id&limit=10000").catch(()=>[])]);return{total_keys:a.length,active_keys:b.length,verifications:c.length,products:d.length};}
export async function keyMeta(request,env){const ip=ipOf(request),start=new Date();start.setUTCHours(0,0,0,0);const rows=await sb(env,"logs?select=id&ip_address=eq."+encodeURIComponent(ip)+"&result=eq.VALID&created_at=gte."+encodeURIComponent(start.toISOString())+"&limit=10000").catch(()=>[]);return json({ip,today_gets:rows.length});}
export async function products(env){return json({products:await ensureProducts(env)});}
async function callShortener(slot,destination,env){return shortenWithProvider(slot.provider,slot.token,destination,env);}
function isProviderBlockedError(error){const s=String(error&&error.message||error).toLowerCase();return s.includes("403")||s.includes("block")||s.includes("không khả dụng")||s.includes("từ chối request");}
export async function startGetKey(request,env){const b=await request.json().catch(()=>({})),pid=Number(b.product_id),ip=ipOf(request);if(!Number.isSafeInteger(pid)||pid<1)return json({error:"Sản phẩm không hợp lệ."},400);if(isBadUserAgent(request)){await registerViolation(env,ip,"bad_user_agent","get-key");return json({error:"Trình duyệt hoặc công cụ truy cập không được hỗ trợ."},403);}if(await banned(env,ip)||await autoBanned(env,ip))return json({error:"IP của bạn đã bị chặn tạm thời hoặc vĩnh viễn khỏi hệ thống."},403);if(!(await consumeRateLimit(env,"get-key",ip,300,5))){await registerViolation(env,ip,"rate_limit","get-key");return json({error:"Bạn đã yêu cầu quá nhiều lần. Vui lòng thử lại sau vài phút."},429);}const p=await sb(env,"products?select=id&id=eq."+pid+"&limit=1");if(!p.length)return json({error:"Sản phẩm không tồn tại."},404);const client=await sha256(ip),token=crypto.randomUUID()+crypto.randomUUID();try{await rpc(env,"create_key_token",{p_token_hash:await sha256(token),p_product_id:pid,p_client_hash:client,p_ttl_seconds:900});}catch(e){await securityLog(env,"key_token_create_error",ip,String(e&&e.message||e));return json({error:"Không thể khởi tạo phiên lấy Key. Vui lòng thử lại."},503);}const original=new URL(request.url).origin+"/key.html?step=3&token="+encodeURIComponent(token);let current=original;const slots=Object.values(await getSlots(env)).filter(x=>x.enabled).sort((a,b)=>a.position-b.position);if(!slots.length)return json({error:"Chưa có nguồn vượt link nào được bật. Vui lòng thử lại sau."},503);const failures=[];let usedProvider=null;for(let i=0;i<slots.length;i++){const s=slots[i];let reserved=false;try{const q=await rpc(env,"reserve_shortener_slot",{p_slot_position:s.position,p_client_hash:client,p_limit:s.quota});const ok=Array.isArray(q)?(q[0]&&(q[0].reserve_shortener_slot??q[0])):(q&&(q.reserve_shortener_slot??q));if(ok!==true&&ok!=="true"){failures.push("Slot "+s.position+" ("+s.provider+"): hết quota");continue;}reserved=true;const next=await callShortener(s,current,env);if(!next)throw new Error(s.provider+" không trả về liên kết");current=next;usedProvider=s.provider;}catch(e){const detail=String(e&&e.message||e).slice(0,300);if(reserved)await rpc(env,"release_shortener_slot",{p_slot_position:s.position,p_client_hash:client}).catch(()=>{});failures.push("Slot "+s.position+" ("+s.provider+"): "+detail);await securityLog(env,isProviderBlockedError(e)?"shortener_upstream_block":"shortener_chain_unavailable",ip,s.provider+": "+detail);if(i+1<slots.length)continue;break;}}if(current===original)return json({error:"Không thể tạo chuỗi vượt link. "+failures.join(" | ")},503);return json({redirect:current,shortener:usedProvider||null});}
export async function claimKey(request,env){const b=await request.json().catch(()=>({})),token=String(b.token||"").trim(),ip=ipOf(request);if(!token)return json({error:"Token không hợp lệ."},400);if(isBadUserAgent(request))return json({error:"Trình duyệt hoặc công cụ truy cập không được hỗ trợ."},403);if(await banned(env,ip)||await autoBanned(env,ip))return json({error:"IP của bạn đã bị chặn."},403);if(!(await consumeRateLimit(env,"claim-key",ip,300,10)))return json({error:"Bạn đã thao tác quá nhanh. Vui lòng thử lại sau vài phút."},429);let rows;try{rows=await rpc(env,"claim_key_with_token",{p_token_hash:await sha256(token),p_client_hash:await sha256(ip),p_duration_hours:10,p_claimed_ip:ip});}catch(e){await securityLog(env,"key_claim_rpc_error",ip,String(e&&e.message||e));return json({error:"Không thể xác nhận Key lúc này. Vui lòng thử lại sau vài giây."},503);}const row=Array.isArray(rows)?rows[0]:rows;if(!row||!row.key_code)return json({error:"Token đã hết hạn hoặc đã được sử dụng. Vui lòng quay lại Bước 1 để lấy lại Key."},403);await sb(env,"logs",{method:"POST",headers:{Prefer:"return=minimal"},body:JSON.stringify({key_code:row.key_code,product_id:Number(row.product_id),ip_address:ip,result:"VALID"})}).catch(()=>{});return json(row);}
export async function checkKey(request,env){const u=new URL(request.url);let key=u.searchParams.get("key")||"",product=u.searchParams.get("product")||"";if(request.method==="POST"){const b=await request.json().catch(()=>({}));key=b.key||key;product=b.product_slug||product;}key=String(key).trim().toUpperCase();if(!key)return json({error:"Thiếu Key."},400);const ip=ipOf(request);if(isBadUserAgent(request))return json({error:"Trình duyệt hoặc công cụ truy cập không được hỗ trợ."},403);if(await banned(env,ip)||await autoBanned(env,ip))return json({error:"IP của bạn đã bị chặn."},403);if(!(await consumeRateLimit(env,"check-key",ip,300,15)))return json({error:"Bạn đã tra cứu quá nhiều lần. Vui lòng thử lại sau vài phút."},429);if(product){const r=await rpc(env,"verify_key",{p_key_code:key,p_product_slug:String(product).toLowerCase()});return json(Array.isArray(r)?r[0]:r);}const r=await rpc(env,"verify_key_public",{p_key_code:key});return json(Array.isArray(r)?r[0]:r);}